import {Meta, Story, Preview, Props} from '@storybook/addon-docs/blocks'
import {action, decorate} from '@storybook/addon-actions'
import {withKnobs, text, boolean, number} from '@storybook/addon-knobs'
import Button from './Button'

<Meta title="Welcome" />

# NextJS Boilerplate

## Example

<Preview>
  <Story name="Example">
    <div className="m-8">
      <button className="hover:bg-green-600 bg-green-500 text-white rounded-md px-8 py-2 border-b-4 border-green-700">
        Save the world
      </button>
    </div>
  </Story>
</Preview>
